<view class="container">
	<van-list loading="{{ loading }}" finished="{{ finished }}">
		<block tt:for="{{ list }}" tt:for-index="idx" tt:for-item="item" tt:key="id">
			<van-cell custom-class="van-cell van-cell--center" title="{{ item }}" label="{{ idx }}" />
		</block>
	</van-list>
</view>